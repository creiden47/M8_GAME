<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.franciscogarzil.m8_game">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_wolf"
        android:label="Wolf Dungeon"
        android:roundIcon="@drawable/logo_wolf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PKG_ACTIVITIES.NewCharacter"></activity>
        <activity android:name=".PKG_ACTIVITIES.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PKG_ACTIVITIES.MainMenu" />
        <activity android:name=".PKG_ACTIVITIES.CharacterPage" />
        <activity android:name=".PKG_GAME.inGame" />
        <activity android:name=".PKG_ACTIVITIES.storeFragment" />
    </application>

</manifest>